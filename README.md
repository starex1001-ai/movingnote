# 이사노트

이사업체 확인부터 주소 변경, 입주 점검까지 준비 순서를 정리합니다.

Build: `python build.py`

Cloudflare Workers Builds: build `python build.py`; deploy `npx wrangler deploy`; branch `main`.

Posts are supplied by the existing Google Sheets publisher. This repository starts with zero posts.
